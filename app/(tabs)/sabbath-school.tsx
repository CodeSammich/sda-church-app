// Keep Sabbath School outside the four visible tab items while rendering it
// inside the tab navigator, so the shared bottom navigation remains visible.
export { default } from './explore/sabbath-school';
