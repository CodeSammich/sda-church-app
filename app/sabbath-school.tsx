// Sabbath School is a standalone destination, not an Explore-tab screen.
// Reuse the existing page implementation while keeping the route outside the
// tab navigator so no bottom tab is highlighted.
export { default } from './(tabs)/explore/sabbath-school';
